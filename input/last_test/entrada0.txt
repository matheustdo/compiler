/*
 * Código com erros
 */

const {
	real a_ = 6;
	boolean c = 6;
	int d = 6;

	real pi, euller = 6;

	pi = 3.14;
	euller = 2.718;
}

var {
	int a_;    //erro
	string b;
	real d; //erro

	int a;
	string c_;

	a = 1;
	f = 2.1;           //erro
	c_ = "hello world";
	g = true;          //erro
}


procedure p(int x, real y) {
	var {
		int valor1, valor2, valor3;
		real b;
		string valor1, valor2; //erro nas 2 variáveis
	}
}

procedure p0() {
	print(a, b);
	return true; //erro
}

function int f(int a) {
	var {
		a = 10;
		b = false; //erro

		real c;
	}

	c = 3.14;
	d = "test"; //erro

	return a;
}

function int f() {
	var {
		real pi, d, e, f;
	}

	pi = 3.14;
	local.d = 2.17;

	global.euller = 2.718; //erro
	f = 0.14;

	return 0;
}

procedure start() {
	b = fb(); //erro
	i = fi(); //erro
	r = fr(); //erro
	s = fs(); //erro
	p0();
	p0(a);    //erro
	
	b = !b || (i > 1) && (r > 1.3) || i == "test"; //erro em i

	s = !b || (i > 1) && (r > 1.3) || s == "test"; //erro

	func(); //erro em func
    func2(a,b); //erro em func2

}



